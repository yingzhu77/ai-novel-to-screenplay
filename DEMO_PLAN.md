# Demo 展示计划书

## 产品定位
AI 驱动的小说转剧本工具，将非结构化小说文本自动转换为结构化 YAML 剧本格式。

---

## 展示流程（建议 3-4 分钟）

### 第一幕：开场（20秒）

**画面：** 首页全貌

**旁白：**
> 这是 AI Screenwriter，一个将小说自动转换为剧本的工具。支持粘贴文本或上传文件，AI 会自动识别章节、提取角色、生成结构化剧本。

---

### 第二幕：短文本演示（60秒）

**画面：** 展示完整流程

**旁白：**
> 支持三种输入方式：粘贴文本、上传文件、拖拽上传。
> 先用一个短篇演示完整流程。

**操作：**
1. 粘贴一段 2-3 章的短篇小说（约 1000 字）
2. 点击"解析章节" → 展示章节列表
3. 点击"转换" → 展示 SSE 流式进度
4. 转换完成 → 展示预览、关系图、下载

---

### 第三幕：长文本演示与局限性（60秒）

**画面：** 上传长篇小说

**旁白：**
> 接下来测试长文本场景。这两篇是约 7000 字的小说，可以看到 AI 处理长文本的效果和局限性。

**操作：**
1. 上传第一篇长文本（约 7000 字）
2. 点击"解析" → 展示章节识别结果
3. 点击"转换" → 观察：
   - 转换时间明显变长
   - 长章节可能出现质量下降或截断
   - 进度条体现逐章处理的耗时
4. 转换完成 → 展示结果，指出可能的不完美之处

**旁白（展示局限性时）：**
> 可以看到，长文本处理存在一些局限：
> - 单章过长时可能影响转换质量
> - 转换时间随文本长度增加
> - 建议将长篇小说拆分为合理长度的章节

---

### 第三幕：章节解析（20秒）

**画面：** 章节列表

**旁白：**
> AI 自动识别章节边界，支持中文"第X章"、英文"Chapter X"、Markdown 标题等格式。解析完成后可以选择要转换的章节。

**操作：** 展示章节列表，全选，点击"转换"

---

### 第四幕：SSE 流式转换（40秒）⭐ 核心卖点

**画面：** 转换进行中（已在第二幕展示，此处可剪辑高光片段）

**旁白：**
> 转换过程通过 SSE 实时推送进度，可以看到：
> - 顶部进度条动态更新
> - 每章状态从"待转换"变为"转换中"再变为"完成"
> - 当前正在处理的章节名称

**操作：** 剪辑第二幕中进度条变化的高光片段

---

### 第五幕：结果预览（50秒）

**画面：** 剧本输出区域，切换各标签

**旁白：**
> 转换完成后，提供多种查看方式：

**操作 1 - 预览标签：**
> 预览标签展示角色卡片和场景对话，角色自动提取姓名、身份、性格特征。

**操作 2 - 关系标签：**
> 关系标签用 SVG 可视化展示角色关系网络，基于场景共现计算关联强度，按角色类型着色。

**操作 3 - 对比标签：**
> 对比标签并排展示原文和剧本，直观看到转换效果。

**操作 4 - YAML/JSON 标签：**
> 结构化数据标签展示完整的 YAML 或 JSON 输出，支持一键复制。

---

### 第六幕：下载与隐私保护（30秒）

**画面：** 下载按钮和云端保存区域

**旁白：**
> 可以直接下载 YAML 或 JSON 文件到本地，无需任何额外操作。
>
> 如果需要跨设备访问，可以勾选同意后保存到云端。我们明确告知用户：内容会存储在共享存储桶中，存在数据泄露风险，建议仅保存非敏感内容。

**操作：**
1. 点击 YAML 下载，展示本地下载效果
2. 展示同意勾选框和提示文案

---

### 第七幕：其他功能（20秒）

**画面：** 深色模式、历史记录

**旁白：**
> 还支持深色模式切换、响应式设计适配移动端，转换记录保存在本地，方便回顾。

**操作：**
1. 切换深色/亮色模式
2. 展示历史记录列表

---

## 技术亮点（供旁白补充）

| 亮点 | 说明 |
|------|------|
| SSE 流式传输 | POST + ReadableStream，逐章推送进度 |
| YAML Schema | 自定义三层结构（meta/characters/chapters） |
| Zod 验证 | LLM 输出实时校验 |
| 角色关系图 | 基于场景共现的 SVG 可视化 |
| 隐私保护 | 手动云端保存，明确风险提示 |

---

## 录制前检查清单

- [ ] **Vercel 重新部署**：当前线上版本未包含最新功能（云端保存、React key 修复）
- [ ] **准备短文本**：2-3 章，约 1000 字
- [ ] **准备长文本**：2 篇约 7000 字的小说
- [ ] **测试转换**：确保 API Key 有效，转换正常工作

## 录制注意事项

1. **先部署再录制**：确保线上版本是最新的
2. **短文本优先**：先展示完整流程，再展示长文本局限性
3. **流式进度是核心**：多停留几秒，让观众看清进度变化
4. **长文本耗时**：7000 字可能需要 1-2 分钟转换，可适当剪辑
5. **深色模式可选**：如果录屏软件背景较暗，用亮色模式更清晰
6. **剪辑建议**：转换等待过程可适当加速，保留开始和结束的完整画面
